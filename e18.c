//С клавиатуры вводится символьная строка, состоящая из слов, разделенных пробелами. Определить количество слов в строке, которые начинаются и заканчиваются на гласную букву. Если таких слов нет - выдать соответствующее сообщение. Вывести на печать исходную строку, найденные слова и их количество.

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
bool gl(char n){
	char G[]={'a', 'e', 'u', 'i', 'o', 'A', 'E', 'U', 'I', 'O'};
	size_t g = sizeof(G)/sizeof(G[0]);
	for(int i=0; i<g; i++){
		if(n==G[i]){
			return true;}}
	return false;}

int main(){
	char A[500];
	printf("Введите строку:\n");
	gets(A);
	puts(A);
	char *b = strtok(A, " \n");
	printf("Полученные слова: ");
	int a=0;
	while(b!=NULL){
		int l = strlen(b);
		if(gl(b[l-1]) && gl(b[0])){
			printf("%s ", b);
			a++;}
		b=strtok(NULL, " \n");}
	if (a==0){
		printf("данные слова отсутствуют");}
	else{printf("\nКоличество данных слов = %d", a);}
	return 0;}

/*
#include <stdio.h>: Подключение стандартной библиотеки ввода-вывода для использования функций, таких как printf, gets, и т.д.
#include <stdlib.h>: Подключение стандартной библиотеки для использования функций общего назначения (в данной программе не используется, можно убрать).
#include <string.h>: Подключение библиотеки для работы со строками (функции strlen, strtok).
#include <stdbool.h>: Подключение библиотеки для использования логического типа bool (значения true и false).
Объявление функции gl
Назначение функции: Проверяет, является ли символ n гласной буквой (включая строчные и заглавные буквы).
char G[] = ...: Создается массив G, содержащий все гласные буквы (строчные и заглавные).
size_t g = sizeof(G) / sizeof(G[0]);:
sizeof(G) возвращает размер массива G (в байтах).
sizeof(G[0]) возвращает размер одного элемента массива (1 байт для символов).
Деление дает количество элементов массива (в данном случае 10).
Цикл for:
Перебираются все элементы массива G.
Если символ n совпадает с одной из гласных, возвращается true (буква является гласной).
Если после проверки всего массива совпадений не найдено, возвращается false (буква не является гласной).
Функция main
Создается массив символов A длиной 500 для хранения введенной строки.
printf: Вывод приглашения для ввода строки.
gets(A): Считывает строку, введенную пользователем, и сохраняет ее в массив A. Важно: использование gets небезопасно, так как функция не проверяет длину ввода. Лучше заменить на fgets.
puts(A): Выводит введенную строку на экран.
strtok: Разделяет строку A на слова, используя пробел (" ") и символ новой строки ("\n") в качестве разделителей.
Возвращает указатель на первое слово.
Если слов больше нет, возвращает NULL.
printf: Выводит текст "Полученные слова: " перед перечислением слов, которые соответствуют условию.
int a = 0: Создается переменная a, которая используется для подсчета количества слов, удовлетворяющих условию.
Вход в цикл while, который выполняется, пока функция strtok возвращает указатель на очередное слово (т.е. пока есть слова для обработки).
strlen(b): Определяет длину текущего слова b (количество символов в слове) и сохраняет результат в переменной l.
Проверяется условие:
gl(b[l - 1]): Последний символ слова проверяется, является ли он гласной.
gl(b[0]): Первый символ слова проверяется, является ли он гласной.
Если оба условия выполняются, слово удовлетворяет критерию.
Если условие выполняется:
printf("%s ", b): Слово выводится на экран.
a++: Счетчик a увеличивается на 1.
Переход к следующему слову:
strtok(NULL, " \n") возвращает указатель на следующее слово в строке.
Если слов больше нет, возвращает NULL, и цикл завершается.
После завершения цикла проверяется значение счетчика a:
Если a == 0, значит, подходящих слов не найдено. Выводится сообщение: "данные слова отсутствуют".
Если значение a больше 0, программа выводит количество найденных слов.
Завершение программы. Возвращение 0 указывает на успешное выполнение.
*/
