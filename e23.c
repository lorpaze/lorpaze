// Дана квадратная целочисленная матрица R(n,n), n <=10. Определить в каждом столбце сумму элементов, кратных 2-м и поместить её на место соответствующего элемента побочной диагонали. Если таких элементов нет, на место диагонального элемента пишется 0. Вывести на печать исходную матрицу, найденное количество для каждого столбца и полученную после постановки матрицу.

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main(){
	srand(time(NULL));
	int nmax=10, n;
	printf("Введите порядок матрицы: ");
	scanf("%d", &n);
	if(n<0 || n>10){
		printf("Ошибка ввода");
		return 0;}
	float A[n][n];
	for(int i=0; i<n; i++){
		for(int j=0; j<n; j++){
			A[i][j]=rand()%21-10;}}
	printf("Исходная матрица:\n");
	for(int i=0; i<n; i++){
		for(int j=0; j<n; j++){
			printf("%4.f ", A[i][j]);}printf("\n");}
	float s=0;
	for(int j=0; j<n; j++){
		for(int i=0; i<n; i++){
			if((int)A[i][j]%2==0){
				s+=A[i][j];}}
		printf("Сумма элементов, кратных 2-м, в %d-м столбце: %2.f\n", j+1, s);
		A[n-1-j][j]=s;
		s=0;}
	printf("Новая матрица:\n");
	for(int i=0; i<n; i++){
		for(int j=0; j<n; j++){
			printf("%4.f ", A[i][j]);}printf("\n");}
	return 0;}

/*
#include <stdio.h>

#define MAX_SIZE 10

int main() {
    int n;
    int R[MAX_SIZE][MAX_SIZE]; // Матрица
    int sum; // Сумма элементов, кратных 2, для текущего столбца

    // Ввод размера матрицы
    printf("Введите размер матрицы (n <= 10): ");
    scanf("%d", &n);

    if (n > MAX_SIZE || n <= 0) {
        printf("Некорректный размер матрицы!");
        return 1;
    }

    // Ввод элементов матрицы
    printf("Введите элементы матрицы %dx%d:\n", n, n);
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            scanf("%d", &R[i][j]);
        }
    }

    // Вывод исходной матрицы
    printf("Исходная матрица:\n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            printf("%4d", R[i][j]);
        }
        printf("\n");
    }

    // Обработка столбцов: суммируем элементы, кратные 2, и записываем их в побочную диагональ
    for (int j = 0; j < n; j++) {
        sum = 0; // Обнуляем сумму для текущего столбца
        for (int i = 0; i < n; i++) {
            if (R[i][j] % 2 == 0) { // Проверка на кратность 2
                sum += R[i][j];
            }
        }

        // Записываем сумму или 0 на место элемента побочной диагонали
        R[j][n - 1 - j] = sum;
    }

    // Вывод измененной матрицы
    printf("\nМатрица после обработки:\n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            printf("%4d", R[i][j]);
        }
        printf("\n");
    }

    return 0;
}

Объяснение программы:
Программа проходит по каждому столбцу.
Суммируются элементы, кратные 2, для текущего столбца.
Если таких элементов нет, сумма остается равной 0.
Сумма записывается на место элемента побочной диагонали (индекс элемента побочной диагонали: [j][n - 1 - j]).
Вывод измененной матрицы:

После обработки выводится новая матрица, в которой элементы побочной диагонали заменены на суммы.
*/
